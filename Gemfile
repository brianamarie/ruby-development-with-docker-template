# frozen_string_literal: true

source 'https://rubygems.org'

# if you want to specify the ruby version that this container & its
# dependencies should be using, do it here
# ruby '~> 2.6.6'

# add gems in this file like this one:
gem 'minitest'

# NOTE: don't remove minitest above - we're going to use it for the next step!
